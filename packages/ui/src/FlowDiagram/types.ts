/**
 * Type definitions for FlowDiagram component
 */

export interface FlowDiagramProps {
  /** Additional CSS classes */
  className?: string;

  /** Animation speed in milliseconds (default: 5000) */
  speed?: number;

  /** Whether to start animation immediately (default: true) */
  autoPlay?: boolean;
}
